
-- appName = 'Notes'
-- appWatchEvent = hs.application.watcher.activated
-- appObj = nil

-- appWatcher = hs.application.watcher.new(function(appName, appWatchEvent, appObj)
--     -- shot = hs.application.find('Notes')
--     print(appName .. ' app ' .. appWatchEvent)
--     print(appObj)
-- end)

-- print('starting app watcher')
-- appWatcher:start()